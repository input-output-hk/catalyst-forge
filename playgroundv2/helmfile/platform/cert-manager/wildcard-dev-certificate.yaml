apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-projectcatalyst
  namespace: envoy-gateway-system
spec:
  secretName: wildcard-projectcatalyst-tls
  dnsNames:
    - "*.projectcatalyst.dev"
  issuerRef:
    kind: ClusterIssuer
    name: mkcert-ca
