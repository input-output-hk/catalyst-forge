apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: earthly-buildkitd-cache
  namespace: registry
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  # Use cluster default StorageClass (e.g., local-path in k3d)
